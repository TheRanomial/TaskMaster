{
  "version": 2,
  "builds": [
    {
      "src": "your-build-directory",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["prisma/schema.prisma"],
        "buildCommand": "npm run build && prisma generate"
      }
    }
  ]
}
